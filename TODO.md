# AdminFlow AI 待辦事項

## P1：實作 iPhone「語音備忘錄」會議轉錄

目前狀態：會議頁面的錄音上傳與進度條只是展示流程；程式不會讀取音訊內容，只會產生「轉錄內容已完成」的提示文字。

目標流程：

1. 使用者從 iPhone「語音備忘錄」匯出並上傳錄音。
2. 後端接收音訊並呼叫語音轉文字服務。
3. 將真正的逐字稿顯示在文字框中，允許使用者校正。
4. 使用者確認後，再交給 Gemini 產生摘要、決議、負責人與截止日。

實作需求：

- 優先支援 iPhone 常見的 `.m4a`（AAC）檔案，並保留 `.mp3`、`.wav` 支援。
- 後端驗證副檔名、MIME type、檔案大小與錄音長度，不將 API key 放入前端。
- 顯示上傳、轉錄、完成及失敗狀態，並提供清楚的錯誤訊息與重試方式。
- 逐字稿必須可編輯；未確認前不要自動儲存為正式會議紀錄。
- 暫存音訊應在轉錄完成或失敗後刪除，並記錄個資與公司機密音訊的保存政策。
- 加入一個由 iPhone「語音備忘錄」實際匯出的 M4A 測試檔進行端到端驗證。

驗收條件：

- 上傳有效的 iPhone M4A 後，文字框顯示音訊中的實際內容，而不是固定提示文字。
- 中文姓名、日期、金額、決議及待辦事項可被正確轉錄並交由 Gemini 結構化。
- 不支援、過大或損壞的檔案會顯示可理解的錯誤，且不影響下一次上傳。
